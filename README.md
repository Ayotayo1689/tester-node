# Microsoft Login Tester (Node.js)

Validates Microsoft account credentials against the account.microsoft.com OAuth login flow.

## Quick Start

```bash
cd microsoft-login-tester-node
docker compose build
docker compose up -d